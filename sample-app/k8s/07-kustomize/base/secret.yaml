# 学習用: 本番ではSealed Secrets / External Secrets でこの YAML をリポジトリに置くのは避ける
apiVersion: v1
kind: Secret
metadata:
  name: todo-secret
type: Opaque
stringData:
  DB_PASSWORD: changeme
  POSTGRES_PASSWORD: changeme
