apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-api
spec:
  template:
    spec:
      containers:
      - name: api
        resources:
          requests: {cpu: 200m, memory: 256Mi}
          limits:   {memory: 512Mi}
